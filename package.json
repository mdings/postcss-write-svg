{
  "name": "gh-pages",
  "private": true,
  "dependencies": {
    "dacoda": "^2.0.0",
    "postcss-write-svg": "^2.0.0",
    "postcss": "^5.0.17"
  },
  "scripts": {
    "build": "browserify index.source.js | uglifyjs -c > index.js",
    "watch": "watchify index.source.js -o index.js"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "uglifyjs": "^2.4.10",
    "watchify": "^3.7.0"
  }
}
